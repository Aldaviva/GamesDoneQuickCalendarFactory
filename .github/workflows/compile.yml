name: Compile program

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  compile:
    env:
      ProjectName: GamesDoneQuickCalendarFactory

    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            targetPlatform: linux-x64
          - os: windows-latest
            targetPlatform: win10-x64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Clone
        uses: actions/checkout@v3.5.1
          
      - name: Initialize test reporting
        uses: testspace-com/setup-testspace@v1
        with:
          domain: ${{github.repository_owner}}
      
      - name: Restore
        run: dotnet restore --verbosity normal

      - name: Build
        run: dotnet build --no-restore --configuration Release --verbosity normal

      - name: Test
        run: |
          dotnet test --no-build --verbosity normal --configuration Release --collect:"XPlat Code Coverage" --settings Tests/Tests.runsettings --logger "trx;LogFileName=TestResults.xml"
          echo "TEST_EXIT_CODE=$LASTEXITCODE" >> ${{ env.GITHUB_ENV }}
          cp Tests/TestResults/*/coverage.info Tests/TestResults
          exit 0

      - name: Publish
        run: dotnet publish ${{ env.ProjectName }} --runtime ${{ matrix.targetPlatform }} --no-build --configuration Release -p:PublishSingleFile=false --no-self-contained --verbosity normal

      - name: Upload test report
        run: testspace Tests/TestResults/TestResults.xml

      - name: Upload coverage
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path-to-lcov: Tests/TestResults/coverage.info

      - name: Stop if tests failed
        run: exit ${{ env.TEST_EXIT_CODE }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v3.1.2
        with:
          name: ${{ env.ProjectName }}-${{ matrix.targetPlatform }}
          path: |
            ${{ env.ProjectName }}/bin/Release/net7.0/${{ matrix.targetPlatform }}/publish/${{ env.ProjectName }}.exe
            ${{ env.ProjectName }}/bin/Release/net7.0/${{ matrix.targetPlatform }}/publish/${{ env.ProjectName }}
            ${{ env.ProjectName }}/bin/Release/net7.0/${{ matrix.targetPlatform }}/publish/appsettings.json
            ${{ env.ProjectName }}/bin/Release/net7.0/${{ matrix.targetPlatform }}/publish/web.config
            ${{ env.ProjectName }}/bin/Release/net7.0/${{ matrix.targetPlatform }}/publish/Install service.ps1
            ${{ env.ProjectName }}/bin/Release/net7.0/${{ matrix.targetPlatform }}/publish/gamesdonequickcalendarfactory.service
          if-no-files-found: error